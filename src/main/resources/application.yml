spring:
  thymeleaf:
    # if false, helps to develop view without restart of app
    cache: true
  datasource:
    url: jdbc:postgresql://localhost:5432/tacocloud
    username: user
    password: password
  jpa:
    show-sql: true

logging:
  level:
    org:
      springframework:
        jdbc:
          core: DEBUG